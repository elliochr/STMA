<h1>Current PTO and Sick Day Requests</h1>
<div>
    {{#each request}}
    <div>
        <div class="request">
            <div>Employee's Name: {{this.first_name}} {{this.last_name}} </div>
            <div>Request Date: {{this.request_date}} Request Type:  {{#if this.req_type}} Sick day {{else}} PTO {{/if}} {{this.req_name}}</div>
            <div>Employee's remaining sick days: {{this.sto_available}} Employee's remaining PTO: {{this.pto_available}}
            <div>Request Message:</div>
            <div class="request-message">{{this.req_description}}</div>
	    <div>
	        <a href="#" class="request-button">  Approve Request  </a>
	        <a href="#" class="request-button">  Deny Request  </a>
	    </div>
        </div>
    </div>
    {{/each}}
</div>
