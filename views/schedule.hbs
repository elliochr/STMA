<h1>Schedule</h1>
<div class="container">
    <div class="row">
        <div class="col-md-12 mx-auto text-center">
            <div class="row">
            {{#each schedule}}
                <div class="col-md-2 py-3">
                    <div class="py-1"><h4>{{date}}</h4></div>
                    <div class="">{{start_time}} to {{end_time}}</div>
                </div>
            {{/each}}
            </div>
        </div>
    </div>
</div>